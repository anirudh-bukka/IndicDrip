import React from "react";
import SearchBar from "../components/SearchBar";

function Catalogue() {
    return (
        <div>
            <SearchBar />
            Catalogue
        </div>
    );
};

export default Catalogue;